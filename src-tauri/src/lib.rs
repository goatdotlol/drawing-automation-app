#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    // This is a stub. The actual entry point is in src/main.rs for this configuration.
    // We keep this to satisfy any potential lib target requirements.
}
